scalar Time
scalar UUID

type User {
  id: UUID!
  email: String!
  displayName: String!
  timezone: String!
  createdAt: Time!
}

type AuthPayload {
  user: User!
  accessToken: String!
  refreshToken: String!
}

enum WorkoutStatus {
  IN_PROGRESS
  COMPLETED
}

type Exercise {
  id: UUID!
  name: String!
  category: String!
  muscleGroups: [String!]!
  equipment: String!
  isCustom: Boolean!
}

enum SetType {
  NORMAL
  WARMUP
  DROPSET
  FAILURE
}

type WorkoutSet {
  id: UUID!
  exerciseId: UUID!
  setNumber: Int!
  reps: Int!
  weightKg: Float!
  rpe: Float
  setType: SetType!
  supersetId: UUID
  performedAt: Time!
}

type Workout {
  id: UUID!
  startedAt: Time!
  endedAt: Time
  notes: String!
  status: WorkoutStatus!
  sets: [WorkoutSet!]!
  templateId: UUID
  # Only meaningful while status is IN_PROGRESS — lets someone else watch
  # this workout live via sharedWorkout / sharedWorkoutProgressUpdated.
  shareCode: String
}

type TemplateExercise {
  id: UUID!
  exerciseId: UUID!
  position: Int!
  targetSets: Int!
  targetReps: Int
  supersetGroup: Int
}

type WorkoutTemplate {
  id: UUID!
  name: String!
  createdAt: Time!
  exercises: [TemplateExercise!]!
}

input TemplateExerciseInput {
  exerciseId: UUID!
  targetSets: Int!
  targetReps: Int
  supersetGroup: Int
}

type PersonalRecord {
  id: UUID!
  exerciseId: UUID!
  recordType: String!
  value: Float!
  achievedAt: Time!
  workoutSetId: UUID!
}

type LogSetResult {
  set: WorkoutSet!
  newRecords: [PersonalRecord!]!
}

type WorkoutEdge {
  cursor: String!
  node: Workout!
}

type PageInfo {
  hasNextPage: Boolean!
  endCursor: String
}

type WorkoutConnection {
  edges: [WorkoutEdge!]!
  pageInfo: PageInfo!
}

type ProgressPoint {
  day: Time!
  totalVolume: Float!
  maxWeight: Float!
  setCount: Int!
}

type BodyMetric {
  id: UUID!
  metricType: String!
  value: Float!
  recordedAt: Time!
}

type ProgressionSuggestion {
  suggestedWeightKg: Float!
  suggestedReps: Int!
  reasoning: String!
  basedOnRpe: Float
}

type PlateauStatus {
  isPlateaued: Boolean!
  currentBestKg: Float!
  message: String!
}

type Query {
  me: User
  exercises(search: String): [Exercise!]!
  activeWorkout: Workout
  workoutHistory(first: Int!, after: String): WorkoutConnection!
  personalRecords: [PersonalRecord!]!
  lastSetForExercise(exerciseId: UUID!): WorkoutSet
  workoutTemplates: [WorkoutTemplate!]!

  progressOverTime(exerciseId: UUID!, days: Int!): [ProgressPoint!]!
  volumeTrend(days: Int!): [ProgressPoint!]!
  bodyMetrics(metricType: String!, days: Int!): [BodyMetric!]!

  progressionSuggestion(exerciseId: UUID!): ProgressionSuggestion
  plateauStatus(exerciseId: UUID!): PlateauStatus!

  # Read-only view of someone else's in-progress workout via their share code.
  sharedWorkout(code: String!): Workout
}

type Mutation {
  signup(email: String!, password: String!, displayName: String!): AuthPayload!
  login(email: String!, password: String!): AuthPayload!
  refreshToken(refreshToken: String!): AuthPayload!
  logout(refreshToken: String!): Boolean!

  startWorkout(templateId: UUID): Workout!
  logSet(workoutId: UUID!, exerciseId: UUID!, reps: Int!, weightKg: Float!, rpe: Float, setType: SetType, supersetId: UUID): LogSetResult!
  updateSet(setId: UUID!, reps: Int!, weightKg: Float!, rpe: Float, setType: SetType): WorkoutSet!
  deleteSet(setId: UUID!): Boolean!
  finishWorkout(workoutId: UUID!, notes: String): Workout!
  deleteWorkout(workoutId: UUID!): Boolean!

  createWorkoutTemplate(name: String!, exercises: [TemplateExerciseInput!]!): WorkoutTemplate!
  deleteWorkoutTemplate(templateId: UUID!): Boolean!

  logBodyMetric(metricType: String!, value: Float!): BodyMetric!
}

type Subscription {
  # Streams every set logged (and any PRs it broke) to anyone watching this
  # workout, backed by Redis pub/sub so it fans out across API instances.
  workoutProgressUpdated(workoutId: UUID!): LogSetResult!

  # Same fan-out, addressed by share code instead of workout ID + ownership
  # — how a friend watches your workout live.
  sharedWorkoutProgressUpdated(code: String!): LogSetResult!
}
